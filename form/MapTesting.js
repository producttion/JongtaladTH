import React, { Component } from 'react';
import {
    AppRegistry,
    StyleSheet,
    Text,
    View
} from 'react-native';

export default class MapTesting extends Component {
    render() {
        return (
            <View style={styles.container}>
            </View>
        );
    }
}